# Lesson 05 Cheatsheet

Filter:

    WHERE

Sort:

    ORDER BY

Limit:

    LIMIT

Count:

    COUNT(*)

Sum:

    SUM()

Average:

    AVG()

Maximum:

    MAX()

Minimum:

    MIN()

Group:

    GROUP BY
