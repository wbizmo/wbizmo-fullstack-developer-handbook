# Databases & SQL Module Cheatsheet

CRUD:

    INSERT
    SELECT
    UPDATE
    DELETE

Filtering:

    WHERE

Sorting:

    ORDER BY

Grouping:

    GROUP BY

Join:

    INNER JOIN

Index:

    CREATE INDEX

Schema change:

    ALTER TABLE

Migration:

    Version-controlled schema update
