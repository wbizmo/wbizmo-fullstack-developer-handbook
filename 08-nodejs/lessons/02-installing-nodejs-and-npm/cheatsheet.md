# Lesson 02 Cheatsheet

Check Node:

    node --version

Check npm:

    npm --version

Initialize:

    npm init -y

Install:

    npm install package

Dev dependency:

    npm install -D package

Key takeaway:
    npm manages packages and projects.
